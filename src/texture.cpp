# include <vecmath.h>
# include <vector>
# include "texture.hpp"
using namespace std;

Vector3f Texture::color(Vector3f in_pos, Vector3f in_direction, vector<Vector3f> out_direction, vector<Vector3f> out_color){
    // return the color of the in ray
    // given the direction of the out ray, and their colors
    return Vector3f();
}